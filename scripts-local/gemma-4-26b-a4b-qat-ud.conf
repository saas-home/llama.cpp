# ==============================================================================
# Model: Gemma-4-26B-A4B-IT-QAT-UD (Q4_K_XL, KV Cache in VRAM)
# Optimized for: 16GB RTX 4070 Ti Super | AMD 7950X3D | 64GB DDR5 RAM
# Architecture: MoE (26B params, 4 active experts) | Q4_K_XL quantization
# KV Strategy: KV cache in VRAM via --kv-offload (default), q4_0 precision
# ==============================================================================

SERVICE_NAME="llama-gemma-4-26b-qat-ud.service"
MODEL_PATH="/home/siva/models/gemma-4-26b-a4b-qat/unsloth/gemma-4-26B-A4B-it-qat-UD-Q4_K_XL.gguf"
MMPRJ_PATH="/home/siva/models/gemma-4-26b-a4b-qat/unsloth/mmproj-F16.gguf"
MODEL_ALIAS="Gemma4-26B-QAT-UD"

# --- COMPUTE ---
# Cross-CCD string for maximum single-model burst performance
CPU_AFFINITY="0-31"  # 16 threads on CCD0 and CCD2 (AMD 7950X3D)
THREADS=16
THREADS_BATCH=16
N_GPU_LAYERS=999
# 8 MOE layers on CPU to free VRAM for q4_0 KV cache, 18 layers on GPU
N_CPU_MOE=4
BATCH_SIZE=2048
UBATCH_SIZE=512
MLOCK=false
FLASH_ATTN=on
NUMA=isolate
MMPRJ_OFFLOAD=false
LOG_DISABLE=true

# --- MEMORY (KV in VRAM, q4_0 Precision, 256K Context) ---
# KV cache allocated on GPU (default kv-offload), q4_0 for VRAM efficiency
CACHE_TYPE_K="q4_0"
CACHE_TYPE_V="q4_0"
CTX_SIZE=262144  # 256K total context
PARALLEL=2       # Two independent conversation slots
PORT=8080

# --- KV CACHE & PROMPT RETENTION ---
# KV_OFFLOAD=true (default) — KV cache goes to GPU with model layers
# kv-unified=true required for --cache-idle-slots (saves idle slots to RAM prompt cache)
KV_OFFLOAD=true
KV_UNIFIED=true
CACHE_RAM_MIB=24576   # 24 GB prompt cache for multi-turn retention
CACHE_IDLE_SLOTS=true # Auto-save idle slots to prompt cache
CONTEXT_SHIFT=true    # Shift context on slot reuse
SLOT_SAVE_PATH="/home/siva/.cache/llama-slots"  # SSD persistence via /slots/save API
CONT_BATCHING=true    # Continuous batching for throughput

# --- INFRASTRUCTURE ---
JINJA=true
#JINJA_KWARGS='{"preserve_thinking": true}'


# --- REASONING ---
REASONING="on"
REASONING_FORMAT="auto"
REASONING_BUDGET=-1
REASONING_BUDGET_MESSAGE=" [Logic Finalized] "

EXTRA_ARGS="--api-key saashome-llama-key-2026" 
#EXTRA_ARGS="--model-draft /home/siva/models/gemma-4-26b-a4b-qat/unsloth/gemma-4-26b-A4B-it-assistant-Q4_0.gguf --spec-type draft-mtp --spec-draft-n-max 4"
